# Project Rules for taka-tales

## Protected Voice Profiles (NEVER MODIFY OR DELETE)
The following voice profiles in `voices/` and `~/.taka-agent/voices/` are protected and MUST NEVER be deleted, modified, or altered:

1. **Nam Đạo Lý** (ID: `nam-dao-ly`)
   - Standard `ref_text` / `ref.txt`: `Xin chào, đây là giọng đọc mẫu từ gờ o gờ chấm zone. Chúc bạn đọc truyện vui vẻ!`
2. **Nữ Đọc Truyện** (ID: `nu-doc-truyen`)
   - Standard `ref_text` / `ref.txt`: `Xin chào, đây là giọng đọc mẫu từ gờ o gờ chấm zone. Chúc bạn đọc truyện vui vẻ!`

Do NOT alter, remove, or modify these voice directories or their contents under any circumstances without explicit user request.
